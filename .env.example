PORT=
NODE_ENV=

# Database Configuration
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=

# Prisma – use same credentials as DB_* above
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=""

# Redis Configuration (use REDIS_PASSWORD in production for secure Redis)
REDIS_HOST=
REDIS_PORT=
REDIS_USER=default
REDIS_PASSWORD=

# OpenAI (for AI / LangChain)
OPENAI_API_KEY=

# Auth (JWT)
JWT_SECRET=
JWT_EXPIRES_IN=7d
